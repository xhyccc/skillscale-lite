# SkillScale — Unified Python Dependencies
#
# Install: pip install -r requirements.txt
# (Individual component requirements.txt files still exist for
#  Docker / CI jobs that only need a subset.)

# Core (agent + gateway + SDK)
pyyaml>=6.0
uuid6>=2024.1.12
aiohttp>=3.9

# Gateway / APIs
fastapi>=0.100.0
uvicorn>=0.20.0
python-dotenv>=1.0.0
openai>=1.0.0
a2a-protocol>=0.1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.2
aiokafka>=0.10.0
aiokafka>=0.10.0
